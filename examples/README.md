# Examples

These files demonstrate the expected shape of plans, reviews, actions, and
decision records without becoming active project history.

Copy or adapt an example only when it represents real project work. Active
records belong in `plans/`, `reviews/`, `actions/`, and `docs/decisions/`.
