# Copy this file to a local .env file only if the eventual project needs one.
# Keep real secrets out of git.
#
# Document variables in this file using safe placeholder values:
#
# VARIABLE_NAME=replace-with-safe-placeholder
#
# Do not add project-specific variables until the project has a real runtime,
# service, tool, or integration that needs them.
