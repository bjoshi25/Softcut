# Softcut local runtime variables (safe placeholders only).
# Never commit real credentials.

# Optional: needed only for WhisperX diarization in Step 1+.
HUGGINGFACE_TOKEN=replace-with-safe-placeholder

# Frontend API target for apps/web (Next.js).
NEXT_PUBLIC_API_BASE=http://localhost:8000
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
